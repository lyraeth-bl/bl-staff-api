$kind: http-request
method: POST
url: "{{base_url}}/api/v1/auth/logout"
examples: .resources/Logout.resources/examples
order: 3000
description: Revokes the current user's access token. Requires Bearer token authentication.
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
